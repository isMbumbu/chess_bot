services:
  api:
    build: .
    env_file:
      - path: .env
        required: false
    environment:
      DATABASE_URL: sqlite+aiosqlite:////data/chess_bot.db
    ports:
      - "8000:8000"
    volumes:
      - chess_bot_data:/data

volumes:
  chess_bot_data:
